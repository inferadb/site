# Set enabled: true to show the announcement banner site-wide.
# Change the id when updating the message — this resets the dismiss state.
enabled: true
id: "waitlist-2026"
text: "Lock in launch-day pricing; join the waitlist before general availability."
link_text: ""
link_url: "/waitlist"
