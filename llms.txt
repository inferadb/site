---
permalink: /llms.txt
sitemap: false
layout: null
---
{%- comment -%}
  llms.txt — agent-readable site index, generated at build time from
  _data/nav.yml, _data/docs_nav.yml, _data/dispatch.yml, _data/stats.yml.

  To update this file:
    - For dynamic sections (Product, Solutions, Docs, Optional), edit
      the source data files. Changes propagate automatically.
    - For static sections (elevator pitch, API Quick Reference, IPL
      Syntax, Site Navigation), edit this template directly.
    - The "Last updated" stamp is auto-set to the build date.

  This file follows the proposed standard at https://llmstxt.org/.
{%- endcomment -%}
{%- assign product_group = site.data.nav | where: "title", "Product" | first -%}
{%- assign resources_group = site.data.nav | where: "title", "Resources" | first -%}
# InferaDB

> InferaDB is the authorization database for the agentic era and the regulated enterprise. Fine-grained access control answering "can subject X do action Y on resource Z?" in {{ site.data.stats.perf.check_latency_p99 }} at the 99th percentile, with cryptographic audit by default and per-tenant cryptographic isolation. Built in Rust. Zanzibar-inspired. AuthZEN-compliant. Open core (MIT/Apache 2.0). Three subsystems: Engine (authorization evaluation), Control (tenant administration), Ledger (per-vault blockchain with Merkle proofs). Supports ReBAC, RBAC, and ABAC unified in the Infera Policy Language (IPL). Extensible with sandboxed WebAssembly modules.

InferaDB is pre-GA, raising a $1.5M–$5M seed round. The managed cloud service is in design-partner phase. The open-source core is functional and shipping. Founders built OpenFGA at Auth0 (now CNCF incubating, used in production by NASA, Zapier, Peloton, Grafana Labs, Docker, and Canonical).

Differentiating properties: purpose-built storage engine in Rust (not a general-purpose database), microsecond decisions ({{ site.data.stats.perf.check_latency_p99 }} p99, {{ site.data.stats.perf.throughput_per_sec }} checks/sec on commodity hardware), cryptographic audit by default (hash-chained, Ed25519-signed, Merkle-verifiable — auditors verify without trusting the vendor), per-vault cryptographic isolation (AES-256-GCM, cross-tenant leakage architecturally impossible), Raft consensus for distributed consistency ({{ site.data.stats.perf.global_consistency }} global), strict serializability with revision tokens.

## Product
{% for link in product_group.items %}{% if link.url contains "/product/" %}
- [{{ link.title }}](https://inferadb.com{{ link.url }}): {{ link.desc }}
{%- endif %}{% endfor %}

## Solutions
{% for link in product_group.items %}{% if link.url contains "/solutions/" %}
- [{{ link.title }}](https://inferadb.com{{ link.url }}): {{ link.desc }}
{%- endif %}{% endfor %}

## Docs
{% for group in site.data.docs_nav %}{% for item in group.items %}
- [{{ item.title }}](https://inferadb.com{{ item.url }}/): {{ item.excerpt }}
{%- endfor %}{% endfor %}

## API Quick Reference

Engine REST (port 8080): POST /v1/evaluate (check permission), POST /v1/relationships:write (write tuples), POST /v1/relationships:list, POST /v1/resources/list, POST /v1/subjects/list, POST /v1/simulate, POST /v1/watch (SSE), POST /v1/expand. AuthZEN: POST /access/v1/evaluation, POST /access/v1/evaluations (batch up to 100), POST /access/v1/search/resource, POST /access/v1/search/subject. Health: GET /health/live, /health/ready, /health/startup (no auth). Metrics: GET /metrics (Prometheus).

Control REST (port 9090): POST /v1/auth/register, /login/password, /logout, /verify-email, /cli/authorize (PKCE), /cli/token. CRUD: /v1/organizations, /v1/organizations/{org}/vaults, /v1/organizations/{org}/clients, /v1/organizations/{org}/teams. Tokens: POST /v1/organizations/{org}/vaults/{vault}/tokens, POST /v1/tokens/refresh, POST /v1/token (client assertion).

Ledger gRPC (port 50051): ReadService, WriteService, AdminService, OrganizationService, VaultService, UserService, TokenService, EventsService, HealthService, RaftService.

## IPL Syntax

```
type <name> {
    relation <name>                           // direct tuple
    relation <name> = <expr>                  // computed
    forbid <name>                             // explicit deny (evaluated first)
    permission <name> = <expr>                // composed permission
}

Expressions: a | b (union/OR), a & b (intersection/AND), a - b (exclusion), viewer from parent (tuple-to-userset), parent->can_edit (related object), module("name") (WASM)
Wildcards: user:* for public resources
Evaluation: forbid rules first, then permits. Union short-circuits on first Allow. Intersection short-circuits on first Deny.
```

## Site Navigation

- [Homepage](https://inferadb.com/): Product overview, microsecond performance, three architectural pillars, use cases
- [Pricing](https://inferadb.com/pricing/): Transparent pricing — Free, $29/mo Launch, $499/mo Scale, custom Enterprise; usage-based, no per-seat
- [Docs](https://inferadb.com/docs/): Technical documentation hub
- [Dispatch](https://inferadb.com/dispatch/): Engineering deep-dives, market commentary, AI/agent posts, news
- [Changelog](https://inferadb.com/changelog/): Product updates in chronological timeline
- [About](https://inferadb.com/about/): Team (Evan Sims CTO, Jane Sims CEO), design principles, founding story
- [Why InferaDB](https://inferadb.com/why/): The first-principles thesis
- [What is Authorization?](https://inferadb.com/learn/authorization/): Plain-language intro to RBAC, ReBAC, ABAC
- [Careers](https://inferadb.com/careers/): Open roles
- [Contact](https://inferadb.com/contact/): Contact form
- [Waitlist](https://inferadb.com/waitlist/): Early-access signup for managed cloud
- [Facts](https://inferadb.com/facts/): Plain-text canonical facts about InferaDB for agent grounding
- [Spec](https://inferadb.com/spec/): Technical specification summary for integration agents
- [GitHub](https://github.com/inferadb): Source code — engine, control, ledger, cli, dashboard, sdks, terraform-provider, deploy

## Optional
{% for post in site.data.dispatch %}
- [{{ post.title }}](https://inferadb.com/dispatch/{{ post.slug }}/): {{ post.excerpt }}
{%- endfor %}

---

Last updated: {{ site.time | date: "%Y-%m-%d" }}. Generated automatically from _data/nav.yml, _data/docs_nav.yml, _data/dispatch.yml, and _data/stats.yml. For the longer one-fetch reference, see [/llms-full.txt](https://inferadb.com/llms-full.txt).
